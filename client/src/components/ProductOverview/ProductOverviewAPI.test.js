import API from './ProductOverviewAPIUtils';
import axios from 'axios';
jest.mock('axios');

describe('API methods', () => {
  it('should get response from getProductReviews', done => {
    expect(true).toBeTruthy();
    done();
  });
});